
<!DOCTYPE html>
<html lang="en">
  <head>
    <script src="app.js" defer></script>
    <link rel="stylesheet" href="style.css" />
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Project</title>
  </head>
  <body>
    <h1>The Jake Spectrum</h1>
    <div id="container" aria-label="polite">
      <h2>
        Sign up here to keep up to date and feel free to sign up for our monthly
        extra in the mail!($0.90/monthly)
      </h2>
      <p>Extras include, but are not limited to, plushies, pens, and pencils</p>
      <form id="signUpForm" method="post" action="">
        <fieldset>
          <legend>Sign up here! Card details are on the bottom!</legend>
          <p><strong>*</strong> indicates a required field below</p>
          <strong><span aria-label="required">*</span></strong>
          <label for="username">Username:</label>
          <input
            type="text"
            id="username"
            name="user_name"
            minlength="5"
            maxlength="20"
            pattern="^[A-Za-z0-9]{5,20}+$"
            required
          />
          <strong><span aria-label="required">*</span></strong>
          <label for="password">Password:</label>
          <input
            type="password"
            id="password"
            name="user_password"
            placeholder="password"
            value="password"
            minlength="8"
            maxlength="20"
            pattern="^[A-Za-z0-9._!$\\\]{8,20}+$"
            required
          />
          <strong><span aria-label="required">*</span></strong>
          <label for="confirmPassword">Confirm Password:</label>
          <input
            type="password"
            id="confirmPassword"
            name="user_confirmPassword"
            value="password"
            minlength="8"
            maxlength="20"
            pattern="^[A-Za-z0-9._!$\\\]{8,20}+$"
            required
          />
          <p>
            Password accepts letters,numbers and symbols and please keep up to
            20 characters max, but 8 characters minimum
          </p>

          <strong><span aria-label="required">*</span></strong>
          <label for="email">Email Address:</label>
          <input
            type="email"
            id="email"
            name="user_email"
            pattern="^[a-zA-Z0-9._%+-]+@[a-zA-Z0-9._%-]+\-[a-zA-Z]{2,3}"
            required
            placeholder="john@example.com"
          />
          <strong><span aria-label="required">*</span></strong>
          <label for="confirmEmail">Confirm Email Address:</label>
          <input
            type="email"
            id="confirmEmail"
            name="user_email"
            pattern="^[a-zA-Z0-9._%+-]+@[a-zA-Z0-9._\%-]+\-[a-zA-Z]{2,3}+$"
            required
            placeholder="john@example.com"
          />

          <strong><span aria-label="required">*</span></strong>
          <label for="phoneNumber">Phone Number:</label>
          <input
            type="tel"
            id="phoneNumber"
            name="user_phoneNumber"
            pattern="^d\{3}-\d{3}-\d{4}+$"
            required
          />
          <strong><span aria-label="required">*</span> </strong>
        </fieldset>
      </form>
    </div>
  </body>
</html>
